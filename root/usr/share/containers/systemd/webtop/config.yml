credentials-file: /etc/cloudflared/credentials.json
ingress:
  - service: https://localhost:3001
    originRequest:
      # webtop serves HTTPS with a self-signed cert
      noTLSVerify: true
      access: *cf_access
