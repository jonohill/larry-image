[Unit]
Description=hermes-agent

[Pod]
PodName=hermes-agent
Network=hermes.network

[Service]
# Mint API key shared between hermes/caddy
ExecStartPre=/usr/bin/sh -c 'install -d -m 0755 /run/hermes-agent; test -s /run/hermes-agent/api.env || { umask 077; printf "API_SERVER_ENABLED=true\nAPI_SERVER_KEY=%s\n" "$$(LC_ALL=C tr -dc a-f0-9 </dev/urandom | head -c 64)" > /run/hermes-agent/api.env; }'
